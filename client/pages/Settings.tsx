import { Layout } from "@/components/Layout";

export default function Settings() {
  return (
    <Layout>
      <h2 className="text-xl font-semibold text-slate-200">Server Settings</h2>
    </Layout>
  );
}
