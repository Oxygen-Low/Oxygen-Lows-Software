## 2024-05-18 - Support Ticket Form Actions\n**Learning:** The Support Ticket view had icon-only buttons (Back and Send) without accessible labels, and the Send button lacked visual feedback during async submission, though the form input was disabled correctly.\n**Action:** Added `aria-label`s to both icon buttons and a loading spinner state using `lucide-react` `Loader2` during message transmission to improve user feedback and screen reader experience.
